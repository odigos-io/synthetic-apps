apiVersion: odigos.io/v1alpha1
kind: Source
metadata:
  name: instrument-tail-sampling
  namespace: java-tail-sampling
spec:
  workload:
    kind: Deployment
    name: tail-sampling
    namespace: java-tail-sampling

kind: Deployment
apiVersion: apps/v1
metadata:
  name: url-templatization
  labels:
    app: url-templatization
    url-templatization-default: "on"
spec:
  selector:
    matchLabels:
      app: url-templatization
  template:
    metadata:
      labels:
        app: url-templatization
        url-templatization-default: "on"
    spec:
      containers:
        - name: url-templatization
          image: p0xd21zf5r.registry.depot.dev/synthetic-apps:nodejs-url-templatization
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SELF_HOST
              value: "127.0.0.1"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
---
kind: Service
apiVersion: v1
metadata:
  name: url-templatization
spec:
  selector:
    app: url-templatization
  ports:
    - protocol: TCP
      port: 8080

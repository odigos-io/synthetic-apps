# Exercises conflicting defaultTemplatization Action rules on the same Deployment.
kind: Deployment
apiVersion: apps/v1
metadata:
  name: url-templatization-multi-rules
  labels:
    app: url-templatization-multi-rules
    url-templatization-scenario: multi-rules
spec:
  selector:
    matchLabels:
      app: url-templatization-multi-rules
  template:
    metadata:
      labels:
        app: url-templatization-multi-rules
        url-templatization-scenario: multi-rules
    spec:
      containers:
        - name: url-templatization
          image: ghcr.io/odigos-io/synthetic-apps/nodejs-url-templatization:url-templatization
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SELF_HOST
              value: "127.0.0.1"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
---
kind: Service
apiVersion: v1
metadata:
  name: url-templatization-multi-rules
spec:
  selector:
    app: url-templatization-multi-rules
  ports:
    - protocol: TCP
      port: 8080

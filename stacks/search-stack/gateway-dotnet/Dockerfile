FROM mcr.microsoft.com/dotnet/sdk:8.0 AS builder
WORKDIR /app
COPY search-stack/gateway-dotnet/SearchGateway.csproj .
RUN dotnet restore
COPY search-stack/gateway-dotnet/ .
RUN dotnet publish -c Release -o /out

FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY --from=builder /out .
EXPOSE 8080
CMD ["dotnet", "SearchGateway.dll"]

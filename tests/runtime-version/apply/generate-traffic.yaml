apiVersion: batch/v1
kind: Job
metadata:
  name: generate-traffic
  namespace: odigos-test
spec:
  template:
    metadata:
      annotations:
        workload: job
      labels:
        app: generate-traffic
    spec:
      restartPolicy: Never
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      containers:
        - name: generate-traffic
          image: curlimages/curl:8.4.0
          imagePullPolicy: IfNotPresent
          command: [ "sh", "-c" ]
          args:
            - |
              set -e
              curl -s --fail http://versionlatest.nodejs:8080
              curl -s --fail http://versionminimum.nodejs:8080
              curl -s --fail http://versionunsupported.nodejs:8080
              curl -s --fail http://versionveryold.nodejs:8080

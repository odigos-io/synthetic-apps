apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: tail-sampling
spec:
  description: |
    E2E tests for Odigos tail sampling.
    Prerequisites: a kind cluster with Odigos already installed at the version under test.
    Configure the language under test via --values (e.g. make test-tail-sampling LANGUAGE=nodejs).
  skipDelete: true
  bindings:
    - name: tests_namespace
      value: (join('-', [$values.language, 'tail-sampling']))
    - name: metrics_baseline_file
      value: (join('', ['./.trace-sampling-metrics-baseline-', $values.language, '.json']))
  steps:

    - name: Create Tests Namespace
      try:
        - script:
            workDir: ../../
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
              - name: DEPOT_SYNTHTIC_APPS_PULL_TOKEN
                value: ($values.depot_pull_token)
            content: |
              make ensure-namespace NAMESPACE="$TESTS_NAMESPACE" DEPOT_SYNTHTIC_APPS_PULL_TOKEN="$DEPOT_SYNTHTIC_APPS_PULL_TOKEN"

    - name: Enable sampling dry run
      try:
        - apply:
            file: ./apply/odigos-sampling-dry-run-config.yaml

    - name: Install Simple Trace Db
      try:
        - apply:
            file: ../common/apply/simple-trace-db-deployment.yaml
            template: true

    - name: Add Destination
      try:
        - apply:
            file: ./apply/simple-trace-db-destination.yaml
            template: true

    - name: deploy test application
      try:
        - script:
            timeout: 5m
            env:
              - name: LANGUAGE
                value: ($values.language)
              - name: DEPOT_SYNTHTIC_APPS_PULL_TOKEN
                value: ($values.depot_pull_token)
            content: |
              #!/bin/bash
              set -euo pipefail

              repo_root="$(cd ../.. && pwd)"
              app_dir="${repo_root}/${LANGUAGE}/tail-sampling"
              if [ ! -d "${app_dir}" ]; then
                echo "unknown language: ${LANGUAGE} (expected ${app_dir})"
                exit 1
              fi
              if [ ! -d "${app_dir}/deployments/tail-sampling" ]; then
                echo "language ${LANGUAGE} does not support tail-sampling (no deployments/tail-sampling)"
                exit 1
              fi

              kubectl get namespace odigos-system >/dev/null
              echo "deploying tail-sampling app from ${app_dir}"
              cd "${app_dir}"
              if [ -n "${DEPOT_SYNTHTIC_APPS_PULL_TOKEN:-}" ]; then
                make apply
                make rollout
              else
                make deploy
              fi

    - name: apply data streams to sources
      try:
        - script:
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
            content: |
              ../common/apply/apply-data-stream-to-sources.sh

    - name: assert workload collector sampling config
      try:
        - assert:
            timeout: 30s
            file: ./assert/workload-collector-config.yaml
      catch:
        - script:
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
            content: |
              kubectl get instrumentationconfig -n "$TESTS_NAMESPACE" -o yaml
              kubectl get sampling -n odigos-system -o yaml

    - name: wait for pods to be instrumented
      try:
        - script:
            timeout: 5m
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
            content: |
              export NAMESPACE="$TESTS_NAMESPACE"
              ../common/assert/wait-for-pods-instrumented.sh
      catch:
        - script:
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
            content: |
              kubectl get pods -n "$TESTS_NAMESPACE" -o yaml
              kubectl get sources,instrumentationconfig -n "$TESTS_NAMESPACE"

    - name: wait for simple trace db
      try:
        - script:
            timeout: 2m
            env:
              - name: TRACE_NAMESPACE
                value: ($namespace)
            content: |
              #!/bin/bash
              set -euo pipefail
              kubectl wait --for=condition=available deployment/simple-trace-db -n "$TRACE_NAMESPACE" --timeout=120s

    - name: record sampling trace metrics baseline
      try:
        - script:
            timeout: 3m
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
              - name: METRICS_BASELINE_FILE
                value: ($metrics_baseline_file)
            content: |
              #!/bin/bash
              set -euo pipefail
              ../common/assert/victoriametrics_sampling_metrics_runner.sh record-baseline \
                ./queries/metrics/trace-sampling-metrics.yaml \
                "$METRICS_BASELINE_FILE"

    - name: trigger traffic
      try:
        - script:
            timeout: 5m
            env:
              - name: LANGUAGE
                value: ($values.language)
            content: |
              #!/bin/bash
              set -euo pipefail

              repo_root="$(cd ../.. && pwd)"
              app_dir="${repo_root}/${LANGUAGE}/tail-sampling"
              cd "${app_dir}"
              make trigger

    - name: Assert expected spans
      try:
        - script:
            timeout: 2m
            env:
              - name: TRACE_NAMESPACE
                value: ($namespace)
            content: |
              #!/bin/bash
              set -euo pipefail
              deadline=$(($(date +%s) + 120))
              while true; do
                if ../common/assert/simple_trace_db_span_query_runner.sh --dir ./queries/spans "$TRACE_NAMESPACE" simple-trace-db; then
                  exit 0
                fi
                if [[ $(date +%s) -ge deadline ]]; then
                  echo "span assertions did not pass within 2m" >&2
                  exit 1
                fi
                sleep 2
              done
      catch:
        - script:
            env:
              - name: TRACE_NAMESPACE
                value: ($namespace)
            content: |
              ../common/assert/simple_trace_db_span_query_runner.sh --dir ./queries/spans --verbose "$TRACE_NAMESPACE" simple-trace-db

    - name: Assert sampling trace metrics
      try:
        - script:
            timeout: 2m
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
              - name: METRICS_BASELINE_FILE
                value: ($metrics_baseline_file)
            content: |
              #!/bin/bash
              set -euo pipefail
              deadline=$(($(date +%s) + 120))
              while true; do
                if ../common/assert/victoriametrics_sampling_metrics_runner.sh assert-delta \
                  ./queries/metrics/trace-sampling-metrics.yaml \
                  "$METRICS_BASELINE_FILE"; then
                  exit 0
                fi
                if [[ $(date +%s) -ge deadline ]]; then
                  echo "sampling trace metrics did not match within 2m" >&2
                  exit 1
                fi
                sleep 2
              done
      catch:
        - script:
            env:
              - name: TESTS_NAMESPACE
                value: ($tests_namespace)
              - name: METRICS_BASELINE_FILE
                value: ($metrics_baseline_file)
            content: |
              #!/bin/bash
              set -euo pipefail
              ../common/assert/victoriametrics_sampling_metrics_runner.sh assert-delta \
                ./queries/metrics/trace-sampling-metrics.yaml \
                "$METRICS_BASELINE_FILE" || true
              echo "--- current totals by rule name (match / keep / drop) ---"
              base="http://odigos-victoriametrics.odigos-system.svc.cluster.local:8428"
              for deployment in tail-sampling-errors tail-sampling-duration; do
                ic="deployment-${deployment}"
                kubectl get instrumentationconfig "$ic" -n "$TESTS_NAMESPACE" -o json | jq -r --arg dep "$deployment" '
                  .spec.workloadCollectorConfig[0].samplingCollectorConfig as $sc |
                  (($sc.highlyRelevantOperations // []) + ($sc.costReductionRules // []))
                  | .[] | "\($dep)/\(.name) id=\(.id)"
                ' | while read -r label; do
                  id="${label##*id=}"
                  for m in match_count_total keep_count_total drop_count_total; do
                    q="sum(otelcol_odigos.sampling.trace.${m}{odigos.sampling.rule.id=\"${id}\"})"
                    echo "${label} ${m}:"
                    kubectl run "vm-debug-$RANDOM" --rm -i --restart=Never -n odigos-system \
                      --image=curlimages/curl:8.4.0 --command -- \
                      curl -sG "${base}/api/v1/query" --data-urlencode "query=${q}" 2>/dev/null | sed 's/pod ".*$//' || true
                    echo ""
                  done
                done
              done
